# resources/dashboard_setup_job.yml
resources:
  jobs:
    my_dashboard_setup:
      name: "[Acme] Setup CPI Dashboard with Ask Genie"
      description: "Creates and publishes the AI/BI Lakeview Dashboard for CPI data with Ask Genie enabled."
      tasks:
        - task_key: create_dashboard
          notebook_task:
            notebook_path: ../src/setup_dashboard.py
