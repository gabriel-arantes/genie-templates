# resources/agent_deploy_job.yml
resources:
  jobs:
    my_agent_deploy:
      name: "[Acme] Log & Deploy CPI Genie Agent"
      description: "Logs the LangGraph agent to MLflow, registers in UC, and deploys to Model Serving."
      tasks:
        - task_key: log_and_deploy
          notebook_task:
            notebook_path: ../src/log_and_deploy.py
